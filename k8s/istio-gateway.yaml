apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: deutsche-spielen-gateway
  namespace: deutsche-spielen
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - deutsche-spielen.plsdontspam.me