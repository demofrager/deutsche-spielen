apiVersion: apps/v1
kind: Deployment
metadata:
  name: deutsche-spielen
  namespace: deutsche-spielen
  labels:
    app: deutsche-spielen
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deutsche-spielen
  template:
    metadata:
      labels:
        app: deutsche-spielen
    spec:
      containers:
      - name: deutsche-spielen-container
        image: registry.plsdontspam.me/deutsche-spielen:latest
        imagePullPolicy: Always
        env:
        - name: LANGUAGETOOL_URL
          value: http://languagetool.deutsche-spielen.svc.cluster.local:8010
        ports:
        - containerPort: 8000
        readinessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 20
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
